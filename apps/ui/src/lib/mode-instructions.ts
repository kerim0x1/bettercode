/**
 * The system instruction the desktop builds for each turn. It lives in
 * @betterc0de/schema/system-instruction, where the backend builds the same
 * instruction for turns from the phone app.
 */
export * from "@betterc0de/schema/system-instruction"
