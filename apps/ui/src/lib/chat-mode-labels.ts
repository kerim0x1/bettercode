/**
 * Display names and normalisation for the chat modes. They live in
 * `@betterc0de/schema/chat-controls`, shared with the phone app; this module
 * keeps the desktop's import path.
 */

export {
  CHAT_MODES,
  chatModeLabel,
  normalizeChatMode,
  type KnownChatMode,
} from "@betterc0de/schema/chat-controls"
