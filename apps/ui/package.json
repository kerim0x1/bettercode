{
  "name": "@betterc0de/ui",
  "version": "0.1.0-beta.4",
  "private": true,
  "type": "module",
  "description": "BetterC0de Vite + React 19 renderer.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b --pretty && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@betterc0de/schema": "*",
    "thinking-orbs": "0.3.1"
  }
}
